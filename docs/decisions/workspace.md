# T04 workspace and capacity decision (23 September 2026)

The checked-in workspace pins pnpm 10.17.1, Node dependencies in `package.json` and `pnpm-lock.yaml`, Rust 1.89.0 in `rust-toolchain.toml`, Anchor 1.1.2 and the exact Rust dependency graph in `Cargo.lock`. `Anchor.toml` and `.env.example` default to localnet. The CrossFlow program ID is `CW1jtAmpZWWwu3HyTACiW6W7Bwh6efcPHiha3noXbRkh`; its signing key remains outside the repository at a 0600 local path. Build products and keypair filenames are ignored. The funded user CLI wallet is separate from the program key. This task performs no on-chain transaction.

The T04 verifier accepts an explicit committed task manifest, checks all selected source files against Git HEAD, runs every mandatory command, rejects a missing or skipped test count, and records hashes and full logs under `artifacts/tasks/T04/`. Its status remains `CHECKS_PASS_REVIEW_PENDING` until independent source/evidence review. CI checks source tests but does not claim to reproduce a local SBF or runtime test; the local task gate builds SBF. Subsequent money-path tasks require their own manifests and runtime evidence.

The capacity probe serializes a three-owner, two-stock-plus-cash maximum envelope with one operator signature, a synthetic lookup table, route accounts and compute-budget instructions. It reserves an *illustrative* 512-byte Pyth verification instruction. It does not establish that a real address lookup table, Pyth payload, route, compute limit or runtime transaction fits. A forced 900-byte mandatory increase must fail. The real transaction and compute envelope is checked again at T17/T23 before demo claims.

The 23 September locked dependency audit reports 0 critical, 0 high and 2 moderate transitive advisories through `@solana/web3.js > jayson`: `uuid` 8.3.2 caller-buffer bounds in v3/v5/v6 and `stream-json` 1.9.1 nested-path filter cost. The current code calls neither API directly and accepts no arbitrary inbound JSON-RPC endpoint. These unresolved findings are recorded for T22 input-boundary review; they are not reported as a clean dependency audit. The scoped `toml` 4.2.0 override removed the two earlier high advisories. The optional `utf-8-validate` peer warning remains subject to the real RPC/WS integration check. No dependency is unpinned to make an audit pass.
