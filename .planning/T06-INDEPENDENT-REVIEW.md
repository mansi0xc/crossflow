# Independent T06 review

**Verdict: PASS for the narrow T06 local thin-settle and recovery slice at implementation commit `7d6a64e` and evidence commit `b1213bd`.** No blocking token-loss, authorization, replay, or accounting bug found. This is a one-owner return of the exact funded asset mix; it does not prove cross-owner matching or conversion.

The owner signer, canonical config/owner-state/intent PDAs, stored owner/config/active intent, Funded status and strict expiry guard protect settlement (`settlement.rs:9-67`). The fixture sequence and full authenticated price guard are checked before transfers. Each configured mint, exact vault ATA and fixed owner recipient ATA are validated; the program returns exactly the booked per-asset funding, enforces each bound, measures exact token deltas, and only then marks claims zero and Settled (`settlement.rs:84-148`). The local negative harness places an invalid third output after two successful token CPIs, then verifies unchanged committed balances. Atomic rollback is therefore exercised in simulation.

Cancellation requires the owner and active intent, changes only status, and is available after expiry without a price, solver or transfer (`recovery.rs:8-32`). Per-asset withdrawal requires the same owner and terminal state, transfers the selected vault's complete current balance to the fixed ATA, verifies exact deltas, and clears that asset claim and surplus (`recovery.rs:34-110`). Close requires zero claims and zero balance/surplus in every canonical vault, closes all three to the owner, decrements the outstanding counter and clears active intent; Anchor closes the intent to the owner (`recovery.rs:113-173`). Persistent next nonce prevents old-intent recreation. Client key order matches the Rust account structs and is exercised by the local path.

**Evidence:** SHA-256 for all 16 listed source files and six check logs matches the final T06 task artifact at `artifacts/tasks/T06/cc456da1615e90215ad5939d906328cf18910ce0d0f9bd279f85c309bc9c98b6`; all six checks pass. The built SBF, deployment manifest and preserved full demo transcript hashes match the runtime evidence. I reran the runtime checker and three client tests. The transcript contains all 15 target-program rejection logs; each third-leg bound failure shows two successful Token Program `TransferChecked` CPIs before CrossFlow rejects the request. Its eight lifecycle signatures and four other nondeploy signatures match normalized evidence; the thirteenth is the program deployment. The offline local ledger genesis matches the evidence; it shows the new program-owned config and closed nonce-0 intent. The final task manifest accurately labels this `LOCAL_VALIDATOR_TRANSCRIPT`, records genesis and 13 signatures, and explicitly says it validates a preserved transcript rather than replaying transactions. Local RPC is no longer running, so I could not independently fetch signatures or re-execute the transactions. This limits independent historical attestation, not the source-bound local proof.

**Mandatory T07 carry-forward:** `WithdrawAsset` currently requires all three owner recipient ATAs as typed Anchor accounts (`recovery.rs:56-61`) even though only one selected asset moves. A missing/closed ATA for any asset therefore prevents withdrawal of the other two, and no fixed-ATA recreation exists yet. `.planning/PLAN-REVIEW.md` PC04 explicitly assigns recipient-ATA recreation to T07. T06's tested per-asset withdrawal with all fixed ATAs present passes, but T07 must fix and test the missing-recipient case before the broader independent-recovery invariant or shared funded demo is claimed. T07 also needs governed policy updates and old-nonce donation recovery.

**Scope limit:** Evidence is isolated localnet with TEST PRICES; no devnet, Pyth, cooperative settlement or external venue has been verified. The current SBF post-processing syscall warning still requires devnet validation before a public demo.
