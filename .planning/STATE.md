# Execution state

Updated: 25 September 2026, after T16 review closure.

- T16 independent review: 3 medium findings, all real and all fixed — (1) the residual decoder read a fixed three weights regardless of `batch_count`, so a two-owner routed body from the shipping client could not execute and a crafted one fed a phantom weight into the allocation; (2) `load_group` was not in fact shared with `settle_batch` and substituted values in the mint-authority slots, making the routed path validate mints MORE weakly than the internal path; (3) the evidence checker compared two generator-written fields instead of recomputing. Six low findings also fixed. `settle_batch` and `settle_routed` now share one loader.
- T32/T18-T20: a bounded local service (`services/api`, loopback only, no keys, no arbitrary command surface) serves `/deployment`, `/price`, `/intents`, `/plans` and `/batches/prepare`; the web app (`apps/web`, React + Vite) implements review → compare → approve → recover. Nine browser tests pass against the real service with an injected wallet stub and a stubbed RPC. Three real bugs were found and fixed while building it: missing CORS, `node:crypto` discriminators (now constants verified by `scripts/check-discriminators.mjs`), and base58 identities passed where the canonical encoder requires hex — the last two crashed loudly rather than signing wrong bytes. The Phantom extension flow and any hosting remain untested.
- T18/T19/T20/T32 verified: `verify:task` reports `CHECKS_PASS_REVIEW_PENDING` for all four (2/2, 3/3, 2/2 and 3/3 mandatory checks). An independent review of the service/UI surface returned REWORK and all ten findings — one high, two medium, seven low — are fixed and re-tested, including a new test that fails every service endpoint and proves owner recovery still works offline.
- T32 live end-to-end: **unfinished**. `scripts/demo-t32-service.ts` drives the real service (plan → intents → prepare → sign → broadcast → reconcile) and the run fixed four genuine service defects — ignored environment variables, base58 identities where the canonical mandate requires hex, a missing compute-budget instruction, and a lookup table that was reported but never used to compile the transaction. The broadcast transaction did not confirm before the run was stopped; no passing evidence was written, and `docs/spec/service-api.md` records exactly where it stops.
- Submission kit: README, `docs/claims-ledger.csv`, `docs/THIRD_PARTY_NOTICES.md`, `docs/evidence/security-matrix.md`, `docs/submission-draft.md`, `docs/demo-script.md`, `docs/submission-checklist.md`, `docs/final-handoff.md`. No video recorded; nothing submitted.

- Execution repository: `/Users/mansitibrewal/chronicles/crossflow`.
- T00: DONE for preflight scope; independent findings resolved and devnet funding prerequisite resolved via user-authorized CLI wallet.
- Current work: T01 scenario input freeze passed independent review and was committed at 9614341; T02 mandate/security specification passed independent re-review after correcting per-cross pricing and task dependencies. T03 passed independent review for the narrow G0 economic mechanism after fixing no-trade attribution; T04 workspace/build harness passed independent review at ac4916b; T14 fixture guard/interface passed independent review; T11 pure proposal validator retains Rust differential/trusted-loader gates; T05 local funding passed its source-bound checks and independent review at 2c811c1; T06 thin settlement/recovery passed all six source-bound checks and independent review at b1213bd. T07 lifecycle/config and missing owner ATA recovery passed seven source-bound checks and independent review at a929cdc. T08 assets/math/dust and old-nonce closed-vault donation recovery passed its six source-bound checks and independent review; T09 bounded three-wallet atomic internal batch settlement passed fourteen source-bound negative cases, two independent-review findings and both medium fixes. G1 is complete; G2 now has an unreviewed-externally internal batch path and still remains open for the composed oracle-plus-route transaction, the adversarial suite and the full money-path review.
- User policy: no routine per-task approval pauses; continue dependency-safe work and report occasionally. Preserve checks and independent reviews.
- Wallet: local CLI wallet FSyL13FTp3Yrgdo8VWpoNtpL8FS5FcSGL3tdNp1sjw2t authorized for CrossFlow devnet; observed balance 20.95376933 devnet SOL at 2026-09-22T15:16:47Z. Phantom browser flow remains to be tested.
- Oracle: fixture prices now; Pyth deferred until core setup, revisit before final demo freeze if pursuing integration.
- Spending: no new real-money spend authorized. No mainnet writes. Use explicit devnet RPC/genesis and verify wallet before signing.
- Known dependency follow-ups: two moderate JS advisories/optional peer warning; revisit in T22. No Rust advisory audit claimed.
- Deadline: 26 September 2026 01:30 IST; internal readiness 25 September 19:30 IST. The old 22 Sep 23:00 IST G0 target was missed; G0 and G1 have now passed late in narrow technical scope. Preserve security/economic gates and cut optional scope first.
- T10: the controlled synthetic residual venue (`programs/test-venue`) builds, executes a real exact-in swap locally (44,379 CU, quote 8,312,489 == measured 8,312,489, moved reserves) and rejects seven named cases. Routing it through CrossFlow settlement is still T16 and unimplemented.
- T21 (held-out, run once, all outcomes preserved): 8 scenarios, 6 feasible across A/B/C, 2 rejected before search; cooperative gain median +16,200 micro-USD, positive in 4 of 6 eligible, never negative; **not robust** — only 36 of 72 declared sensitivity points keep a positive cooperative gain, and one scenario's netting is worse than independent execution. The headline cooperative claim must be narrowed accordingly. T12 engines and T21 evaluation reproduce byte-identically.
- T16 PASS (local scope): `settle_routed` pools every debit, executes the admitted leg against the pinned venue, allocates the measured output pro rata by each owner's own contribution, re-checks the per-owner ±200 bps external band, reconciles the pools against `sum C` and requires them empty. Evidence: one internal cross plus a 50,000-raw-stock leg, 311,591 CU, 43 lookup entries, six named rejections, payouts equal to the independent expectation, 1.28% realized deviation. Review of the T09 policy growth also caught a real regression: boxing `Policy` was required after the route field pushed both handlers past the SBF frame limit, and T09's local evidence was regenerated afterwards. Still local-only; no devnet route.
- T24 devnet probe PASS (probe scope only): program `CW1jt…XbRkh` deployed to public devnet with config `7Fw4sjv…udnS2`; three owners funded their own slices; one explicit internal cross settled atomically at 158,632 CU behind a 33-entry address lookup table; seven invalid variants rejected with named errors; a fresh intent was cancelled, withdrawn per asset and closed, returning exactly 5,000,000 cash and resetting the outstanding-claim counter to zero. No route, no Pyth, no UI. The free public RPC rate-limits heavily and every read is paced with backoff. Upgrade authority is retained and disclosed.
- T09 scope note: settlement is internal-crossings-only. `settle_batch` declares no signer; authority comes only from funded mandates. Three owners need an address lookup table (the identical instruction is 1275 legacy bytes). Single-owner settlement stays on the owner-signed `settle_thin` path.
- Resume: read AGENTS.md and active task evidence; latest concurrency policy supersedes earlier per-task review instructions.
